name: Test Access Secrets

on:
    pull_request:

jobs:
    first:
        runs-on: ubuntu-latest
        steps:
            - name: Use secret
              run: echo "bbb"
              env:
                TOKEN: ${{ secrets.IMPORTANT }}
